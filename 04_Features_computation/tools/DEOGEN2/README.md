## PF Score feature Computation

In order for this folder to work, the user must firstly execute InterPro folder 

After that, the script can be executed with the following command:

```
python3 deogen.py
```

